<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Fields_required_Need_to_Submit_Stage</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
RecordType.Name = &quot;Scale&quot;,
ISPICKVAL(StageName, &quot;Need to Submit&quot;),
OR(
ISBLANK(TEXT(Policy_Type_Status__c)),
ISNULL(Renewal_Date__c),
ISNULL( Number_of_Employees__c ),
ISNULL( Total_Assets__c )
)
)</errorConditionFormula>
    <errorMessage>Expiration Date, Policy Type/Status, Number of Employees &amp; Total Assets must be entered before moving to Need to Submit.</errorMessage>
</ValidationRule>
