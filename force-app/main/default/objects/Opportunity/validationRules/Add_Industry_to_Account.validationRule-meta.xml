<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Add_Industry_to_Account</fullName>
    <active>true</active>
    <description>Updated VR - Jira: SF-1821</description>
    <errorConditionFormula>AND(
    OR(
        ISPICKVAL(StageName, &quot;Need to Submit&quot;),
        ISPICKVAL(StageName, &quot;Proposal Received&quot;)
    ),
    ISCHANGED(StageName),
    $Setup.Master_Validation__c.Account_Active__c,
    OR(
        $RecordType.Name = &quot;BRP&quot;,
        $RecordType.Name = &quot;Founder Shield&quot;
    ),
    ISPICKVAL(Policy_Type_Status__c, &quot;Renewal&quot;),
    NOT(ISNULL(Origin_Policy__c)),
    ISBLANK(TEXT(Account.Industry))
)</errorConditionFormula>
    <errorMessage>Go to the Account page and enter Industry before closing this opportunity.</errorMessage>
</ValidationRule>
