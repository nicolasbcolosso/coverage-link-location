<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Linked_Account_Required</fullName>
    <active>true</active>
    <errorConditionFormula>$Setup.Master_Validation__c.Account_Active__c &amp;&amp; 
NOT(OR($RecordType.Name = &quot;Health &amp; Benefits&quot;, $RecordType.Name = &quot;Scale&quot;, $RecordType.Name = &quot;FS Direct&quot;)) &amp;&amp; 

AND(
ISCHANGED( StageName ) ,
  NOT( OR( 
     ISPICKVAL(StageName, &apos;Need to Submit&apos;),
     ISPICKVAL(StageName, &apos;Need Info From Client&apos;)
  ) ), 
  ISBLANK(AccountId)
)</errorConditionFormula>
    <errorMessage>The Opportunity must have a linked Account to move to the next Stage.</errorMessage>
</ValidationRule>
