<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Quote_must_have_limits</fullName>
    <active>false</active>
    <errorConditionFormula>OR(
  ISCHANGED( Premium__c ),
  OR(
    ISCHANGED( FS_Fees__c ),
    OR(
      ISCHANGED( Policy_Fees__c ),
      ISCHANGED( Wholesale_Fees__c )
    )
  )
)</errorConditionFormula>
    <errorMessage>Quote must have limits before it can be moved to Received Status</errorMessage>
</ValidationRule>
